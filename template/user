COPY amber-image-release /etc

RUN groupadd -g 1096 -r amber && useradd -u 1096 --no-log-init -r -g amber amber

RUN chown amber:amber /opt

USER amber:amber

ENV PATH=/app/local/bin:/app/local/miniconda/bin:$PATH
ENV LD_LIBRARY_PATH=/app/local/lib:/app/local/miniconda/lib:$LD_LIBRARY_PATH
