ARG DOCKER_HUB=""
FROM ${DOCKER_HUB}debian:12

RUN apt update \
 && apt install -y \
        python-is-python3 \
        python3 \
        tcsh make \
        gcc gfortran g++ \
        flex bison patch wget bc \
        libzip-dev xorg-dev libbz2-dev \
        rsync openssh-client

# reconfigure dash to bash
#RUN ln -sf /bin/bash /bin/sh
