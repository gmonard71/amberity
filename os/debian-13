ARG DOCKER_HUB=""
FROM ${DOCKER_HUB}debian:13

RUN apt update \
 && apt install -y \
        python3 \
        tcsh make \
        gcc gfortran g++ \
        flex bison patch wget bc \
        libzip-dev xorg-dev libbz2-dev \
        rsync openssh-client

