ARG DOCKER_HUB=""
FROM ${DOCKER_HUB}opensuse/leap:16.0

RUN zypper -n install tcsh \
                      gcc gcc-fortran gcc-c++ \
                      which patch make \
                      libtool flex bison wget \
                      gzip bzip2 bc \
                      python3 rsync openssh hostname

